{
  "name": "linux-basics-notes",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "lint:md": "markdownlint '**/*.md'",
    "lint:links": "find . -name '*.md' -not -path './node_modules/*' -print0 | xargs -0 -n1 markdown-link-check"
  },
  "devDependencies": {
    "markdown-link-check": "^3.13.7",
    "markdownlint-cli": "^0.41.0"
  }
}
